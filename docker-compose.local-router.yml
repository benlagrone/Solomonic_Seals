services:
  truevineos-local-router:
    image: nginx:1.27-alpine
    container_name: truevineos-local-router
    restart: unless-stopped
    ports:
      - "127.0.0.1:80:80"
    volumes:
      - ./deploy/nginx/truevineos.local.conf:/etc/nginx/conf.d/default.conf:ro

